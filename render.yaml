services:
  - type: web
    name: neuroadaptive-difficulty-scaling
    runtime: static
    buildCommand: echo "Static site — no build step"
    staticPublishPath: .
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
